# Dependencies
api/vendor/
pwa/node_modules/
pwa/dist/

# Environment (contains passwords/tokens)
api/.env
api/.env.backup

# Laravel generated
api/storage/logs/*.log
api/storage/framework/cache/*
api/storage/framework/sessions/*
api/storage/framework/views/*
api/bootstrap/cache/*.php

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
Thumbs.db
.DS_Store

# Claude Code memory (local only)
.claude/

# Archive of old patches (already applied)
_archive/

# Unified firmware (kept for reference, individual files are the ones to flash)
# firmware/crono/

# Old fragment directories (code already deployed to servers)
campeonato-controllers/
campeonato-migrations/
campeonato-models/
controllers/
dispositivos/
heats/
heats_ui/
migrations/
models/
parte2a/
parte2b/
refactor/
sensores/
sponsors-controllers/
sponsors-migrations/
sponsors-models/
sync/
top3/
pwa-src/

# Firmware configs con credenciales (no subir al repo)
firmware/*/config.h
firmware/crono/config_a.h
firmware/crono/config_b.h

# Infra doc with passwords (keep local only)
pioneros_infra_setup.md
